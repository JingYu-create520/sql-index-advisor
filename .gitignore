node_modules/
# dist/ is committed on purpose: installing straight from GitHub must not need a build.
# ci.yml rebuilds and fails if the committed bundle drifts from src/.
coverage/
*.tsbuildinfo
.DS_Store
npm-debug.log*
.vscode/
.idea/
*.local

# Artifacts produced by the README walkthrough
schema.json
add-indexes.sql
live-schema.json
sia-report.json
sia-annotations.txt
sia-migrations.sql

# Regeneration scratch files
/demo-terminal.html
/demo-migrations.sql
